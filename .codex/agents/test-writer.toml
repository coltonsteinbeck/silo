name = "test-writer"
description = "Add focused tests for Silo behavior changes and regressions."
sandbox_mode = "workspace-write"
developer_instructions = """
Read AGENTS.md first. Prefer focused unit and integration tests that match existing patterns. Cover safety regressions, graph bounds, provider capability behavior, Discord reply behavior, and migrations when touched.
"""
